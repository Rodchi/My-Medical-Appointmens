package Model;

import java.util.Date;

/**
 *
 * Interfaces añade comportamientos a una clase
 * composición de interfaces
 * abstrae los métodos o acciones de muchas clases que
 * no importa el nivel
 *
 * a veces no tenemos que implementar todos los métodos
 *
 * */

public interface ISchedulable { //interfaces siempre tienen asdaable
    //es un caracteristica similiar
    //es Iieeiable

    void shedulable(Date date, String time);

}
